<diff case>

# Ctrl-C, D, \
cat 실행 도중 ctrl-C 사용
cat 실행 도중 ctrl-\ 사용
=> bash는 터미널에 뭔가 찍힘

# Double Quotes
"echo"
-> segmentation fault, "hi"같은 문자열도 마찬가지
-> 갑자기 됨, bash와 조금 다름
# Single Quotes
''
-> segmentation fault, 'hi'같은 문자열도 마찬가지
-> 갑자기 됨

# Redirection
cat < a a < b b < c << hi > x >> y
->이건 어떻게 테스트하는건가요?

# else
rm '?'
->segmentation fault

<Whole Case>

# simple command
minishell > /bin/ls
minishell > (enter)
minishell > (space)
minisehll > \t

# Arguments & history
/bin/ls -al
/bin/cat -e Makefile

# echo
echo hello
echo -n hello
echo -nnnnn -n -nnnn hello -n
echo -nnnnnnnm hello
echo hello -n

# exit
exit
exit 100
exit hello

# Return value
/bin/ls
echo $?
/bin/ls nofile
echo $?
expr $? + $?

# Ctrl-C, D, \
빈 프롬프트에 ctrl-C 사용
빈 프롬프트에 ctrl-\ 사용
빈 프롬프트에 ctrl-D 사용

무언가 써있는 프롬프트에 ctrl-C 사용
엔터로 버퍼에 남아있는지 확인
무언가 써있는 프롬프트에 ctrl-\ 사용
무언가 써있는 프롬프트에 ctrl-D 사용

cat 실행 도중 ctrl-C 사용
cat 실행 도중 ctrl-\ 사용
cat 실행 도중 ctrl-D 사용

# Double Quotes
"pwd"
"ls"
"echo"
echo "hello       world"env

# Single Quotes
'ls'
''
echo '$USER'
echo '$HOME   dd'

# env
env

# export
export a=aaaa
export b=bbbb
export c
export abcdef
export
env

# unset
unset a
unset b c
unset abcdef

# cd
cd ..
cd .
cd /Users/eoh

# Relative Path
ls
cat
cat Makefile
mkdir directory
rm -rf directory

# 환경변수 경로(PATH삭제하고 실행)
unset PATH
ls
cat
/bin/ls
export PATH=/bin
ls
cat
sort

# Redirection
echo hello > a
echo world >> a
cat a
cat < a
cat < a > b

cat << end
> aaa
> bbb
> end
aaa
bbb

cat < a a < b b < c << hi > x >> y

# Pipes
cat Makefile | grep @ | more
echo hello > a | cat a | cat < a > b

# history
up & down 으로 history 확인, 실행 가능한지

# Go crazy
> input something (Ctrl-C) (enter)
up & down 으로 history 확인, 실행 가능한지

# 환경변수
export test=pwd
$test
"$test"
echo "$USER"
